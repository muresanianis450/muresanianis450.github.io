<?xml version="1.0" encoding="utf-8"?><!--
   Backup / transfer rules for API 31+.

   Everything is excluded from both cloud backup and device-to-device transfer.
   The archive this app produces is a full copy of the user's SD card and must
   never leave the device this way. See also backup_rules.xml for API 30 and below.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="external" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="external" path="." />
    </device-transfer>
</data-extraction-rules>
